function parsorted = parBubbleSort(matrix)
    N = size(matrix, 2); 
    M = size(matrix, 1);
    for col_j = 1:N
        parfor l = 1:p.Numworkers
                
        end
      end
    end
    for m = 1:M-k
       if matrix(m, col_j) < matrix(m+1,col_j)
        temp = matrix(m,col_j);
        matrix(m,col_j) = matrix(m+1, col_j);
        matrix(m+1, col_j) = temp;
       end
    end
end